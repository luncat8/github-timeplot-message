
same style plot as on github

input line for text or draw rectangles with pointer

slider to move date related to image. horizontally, by set week.

buttons fill, clear
draw tools: draw, erase

slider to adjust draw amount.

amount is relative and draw color related to max amount.
example: 
0,1  -> 0, 255
0,2,3 -> 0, 255/4*3, 255
note typically it is green on white bg similar to github theme.

auto save, load at start localstorage. import, export. Use compact efficient save format 

## get user commits to sync with image plan and suggest todays commit number related to yesterday (high and low levels to print image).

## if text add option subpixel smooth font - to not only use low and high commit numbers but also some other for grayslake antialiasing

##
num input low high commits mapped to level 1 … max 
 duplicate functionality of draw amount slider. better make 2 sliders: for low/erase and high/draw

##
reorder gui according to workflow:
1 fetch ...
2 tools ...
3 text input ...
4 plot 
5 plot info
6 ...

##
after fetch recalculate low and high according to average commits, so it is typically like recommended low 5, high 10, today high so 10 or 0 if antialiasing or graciles image

